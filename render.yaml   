services:
  - type: web
    name: shameapp-backend
    env: node
    rootDir: backend
    buildCommand: "npm install"
    startCommand: "node server.js"
    plan: free
    envVars:
      - key: PORT
        value: 8000
      # Put sensitive values in the dashboard instead of here:
      # - key: MONGO_URI
      #   value: your-connection-string
      # - key: OPENROUTER_API_KEY
      #   value: sk-or-xxxxxxxx
